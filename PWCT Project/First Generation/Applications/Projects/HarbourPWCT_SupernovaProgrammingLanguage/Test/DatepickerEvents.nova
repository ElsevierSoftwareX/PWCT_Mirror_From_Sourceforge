I want window and the window title is "Date Picker Events"
The window width is 230. and the window height is 300.

I want label and the label top is 10.

I want button and the button top is 200.
The button caption is "Exit" and the button name is btn1.

I want datepicker
The date picker top is 50.

I want datepicker
The date picker top is 100.
The date picker left is 50.
The date picker width is 150.
The date picker height is 50.
The date picker fore color is {255,255,255}.
The date picker back color is {0,0,0}.
The date picker font name is "Arial"
the date picker font size is 18.
the date picker font bold is true.
the date picker name is mydate1.

btn1 mouseclick. instructions are
	 you close window
end of instructions

mydate1 gotfocus. instructions are
	the label caption is got focus event.
end of instructions

mydate1 lostfocus. instructions are
	the label caption is lost focus event.
end of instructions

mydate1 change. instructions are
	the label caption is change event.
end of instructions