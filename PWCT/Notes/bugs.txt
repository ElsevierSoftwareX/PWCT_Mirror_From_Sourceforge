[1] Time Machine
create step (print text)
move it up (before wait)
use the time machine to hide print text
run
you will see   no wait



[2] components browser								(SOLVED)
search by keyboard - select unvisible component
example : type "p" will select "progress bar" in controls domain
you don't see this component
what must be selected is "print text to console" component



[3] procedure uses win1.release before define new window win1 lead to syntax error
try to use do method it's better


[4] components browser performance is slow 					(SOLVED)
idea : when you check sub components if the scope of the parent is limited
don't check the parent from the child side it's not required
this is required only when the scope of the parent is general

