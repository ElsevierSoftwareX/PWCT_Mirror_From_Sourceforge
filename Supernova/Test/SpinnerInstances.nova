Set window auto show off

I want window and the window title is "spinner"
The window width is 500.
The window height is 330.

I want label and the label top is 210.
The label width is 300. 
The label font name is "Arial" 
The label font size is 14.

I want button and the button caption is Close.
The button name is mybtn. and the button top is 250.

I want spinner and myspinner init. instructions must done

I want spinner and myspinner init. instructions must done
The spinner left is 230.

I want spinner and myspinner init. instructions must done
The spinner left is 460.

You maximize window and you show window

myspinner init. instructions are
The spinner name is myspinner.
The spinner width is 200.
The spinner height is 40.
The spinner font name is "arial"
The spinner font size is 18.
end of instructions

myspinner change. instructions are
	The label caption is "spinner value changed."
end of instructions

myspinner gotfocus. instructions are
	The spinner forecolor is {255,255,255}.
	The spinner backcolor is {255,0,0}.
end of instructions

myspinner lostfocus. instructions are
	The spinner forecolor is {0,0,0}.
	The spinner backcolor is {255,255,255}.
end of instructions

mybtn mouseclick. instructions are
	You close window
end of instructions

