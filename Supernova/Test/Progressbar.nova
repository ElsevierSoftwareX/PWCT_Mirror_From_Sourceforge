I want window and the window title is "progressbar"
The window width is 430.
The window height is 330.

I want button and the button caption is Close.
The button name is mybtn. and the button top is 250.

I want progressbar and myprogressbar init. instructions must done
The progressbar top is 10. and The progressbar value is 50.

I want progressbar and myprogressbar init. instructions must done
The progressbar top is 50. and The progressbar value is 75.

I want progressbar and myprogressbar init. instructions must done
The progressbar top is 90. and The progressbar value is 25.

I want progressbar and myprogressbar init. instructions must done
The progressbar top is 130. and The progressbar value is 100.

myprogressbar init. instructions are
The progressbar left is 10.
The progressbar width is 400. and the progressbar height is 30.
end of instructions

mybtn mouseclick. instructions are
	You close window
end of instructions